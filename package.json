{
  "name": "liftarchives",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "client",
    "server",
    "shared",
    "database"
  ],
  "scripts": {
    "dev": "bun run --filter '*' dev",
    "build": "bun run --filter '*' build"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
