{
  "name": "@liftarchives/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio",
    "db:drop": "drizzle-kit drop",
    "db:seed": "bun run src/seed-lifts.ts"
  },
  "dependencies": {
    "@liftarchives/shared": "workspace:*",
    "drizzle-orm": "^0.45.1"
  },
  "devDependencies": {
    "better-sqlite3": "^12.6.2",
    "drizzle-kit": "^0.31.8"
  }
}
